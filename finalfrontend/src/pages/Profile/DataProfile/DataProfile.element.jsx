import styled from "@emotion/styled"

const DataProfileStyles = styled.div`


`

export const DataProfileElement = ({children}) => {
  return (
    <DataProfileStyles>{children}</DataProfileStyles>
  )
}
